/** The lead validation asset, badged "Primary" in the portfolio list. Single source of
 * truth until the project schema carries an explicit primary / display-order field (a
 * backend + seed change) — flip it here rather than hunting a literal id through JSX. */
export const PRIMARY_PROJECT_ID = "nur_navoi_solar";
